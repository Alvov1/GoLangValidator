package main
import "string"
func main() {
    nums := []int{2, 3, 4}
    if len(p.Env) == 0 {
        err := os.Symlink(filepath.Join(p.Dir, base), filepath.Join(p.Dir, tmp))
        if err != nil {
            return "", err
        }
        p.tempAppYaml = tmp
        return p.tempAppYaml, nil
    }
}