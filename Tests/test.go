package main
import "string"
func main() {
    nums := []int{2, 3, 4}
    if len(p) {
        err := os.Symlink(filepath.Join(p, base), filepath.Join(p, tmp))
        if err != nil {
            return "", err
        }
        p.tempAppYaml = tmp
        return p.tempAppYaml, nil
    }
}